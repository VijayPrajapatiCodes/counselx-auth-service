apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: counselx
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8081
      targetPort: http
